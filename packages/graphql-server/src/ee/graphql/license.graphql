type License {
  licensedTo: String
  licenseStatus: String
  startedAt: String
  expiredAt: String
  maxUser: Int
  maxGroup: Int
  maxNode: Int
  maxModelDeploy: Int
  usage: Usage
}

type Usage {
  maxUser: Int
  maxGroup: Int
  maxNode: Int
  maxModelDeploy: Int
}
