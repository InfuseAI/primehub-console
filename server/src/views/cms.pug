
extends ./layout.pug

block head
  script.
    window.LOCALE= '#{locale}';
    window.username = "#{username}";
    window.thumbnail = "#{thumbnail}";
block main
  div(id='root')
  script(src="/index.js")
block footerScript
